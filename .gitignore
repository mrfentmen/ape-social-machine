# secrets — NEVER commit
.env
*.pem
*_token*.json
x-bluesky/.env

# machine state (lives locally, not in git)
x-bluesky/bsky_posts_sent.txt
bsky_queue_staging.json
# NOTE: x-bluesky/bsky_scheduled_posts.json IS tracked on purpose:
# the cloud workflow commits posted-flags back so nothing double-posts.

# node
node_modules/

# os junk
.DS_Store

# python
__pycache__/
*.pyc
